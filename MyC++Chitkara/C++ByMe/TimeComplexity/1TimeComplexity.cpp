#include<iostream>
using namespace std;

/*big(O) 
f(n)<=cg(n)
f(n)is a time Complexity
c>=0,n>=n0  if n0 then n0>=0
*/
//it is a class
//1<logn<sqrt(n),nlogn<n*2<n*3.....<2*n<3*n<....n*n
//n*3 ke left side upperbound  and  it will go for big(O) right side lowerbound so it will go for big(omega).
//omega is best case.
//now take closet to class/

/* bog(theta)
c1g(n)>=f(n)>=c.g(n)

So search a class which is in the range at both these class.
*/
int main()
{
    return 0;
}